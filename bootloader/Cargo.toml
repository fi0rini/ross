[package]
name = "bootloader"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
core2 = "0.4"
volatile = { path = "../lib/volatile" }
pi = { path = "../lib/pi" }
xmodem = { path = "../lib/xmodem" }