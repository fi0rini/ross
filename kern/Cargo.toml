[package]
name = "kern"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
volatile = { path = "../lib/volatile" }
pi = { path = "../lib/pi" }